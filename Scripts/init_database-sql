/*
======================================================================
Create Database and Schemas
======================================================================
Script purose:
    This script creates a new database named 'DataWarehouse' after checking if it already exists.
    if the database exsists, it is dropped and recreate. Additionaly, the script sets uo three schemas
    within the database: 'bronze', 'silver', 'gold'.

Warning:
      Running this script will drop the entire 'DataWarehouse' database if it exist.
      All data in the database will be permanently deleted. proceed with caution 
      and ensure you have proper backups before running this script.
*/

  USE master;
  GO

-----Drop and recreate the 'Datawarehouse' database
If exists (select 1 from sys.database where name = 'DataWarehouse')
Begin
    Alter Database DataWarehouse set ssingle_user with Rollback Immediate;
    Drop Database Datawarehouse;
    End;
    go

  ----create Database 'DataWarehouse'

use master;

create Database datawarehouse;
use datawarehouse;

create schema bronze;
go
create schema silver;
go
create schema gold;
go
